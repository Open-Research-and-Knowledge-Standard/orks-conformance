# Schema Ownership

- Status: Reserved for later approved work

This directory is reserved for future JSON Schemas derived from the exact
[pinned standard input](../SUPPORTED-STANDARD.md), using JSON Schema Draft
2020-12 under a separately approved task.

It currently contains no schema, compiler, schema-validation behavior,
runtime selection, dependency, or executable standard interpretation. Future
schemas cannot create, weaken, reinterpret, or silently repair normative
behavior absent from the pinned source tree.

Return to the [documentation index](../docs/README.md).
